// Promoter support tickets reuse the same UI as the seller — same backend
// endpoints, same list/detail/dialog shape. Promoters can raise tickets too.
export { MyTicketsPage as PromoterTicketsPage } from '@/features/seller/support/MyTicketsPage';
