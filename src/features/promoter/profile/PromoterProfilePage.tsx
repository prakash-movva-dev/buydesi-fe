// Promoter profile is identical to the seller's profile UI (name / language /
// zone via /users/me). Re-export the existing component to keep code paths
// shared.
export { ProfilePage as PromoterProfilePage } from '@/features/seller/profile/ProfilePage';
